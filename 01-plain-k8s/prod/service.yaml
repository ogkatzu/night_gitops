apiVersion: v1
kind: Service
metadata:
  name: webapp-prod
  labels:
    app: webapp-prod
spec:
  type: NodePort
  selector:
    app: webapp-prod
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
